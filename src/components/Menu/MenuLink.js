import React from 'react';

        function MenuLink({ to, title}) {
            return (
              <a  href = {to} title = {title}>
              </a>
            );
          }

export default MenuLink;